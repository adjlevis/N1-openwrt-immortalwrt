name: N1 ImmortalWRT docker Build

on:
  push:
    branches: [ main ]  # 仅在 main 分支推送时触发
  workflow_dispatch:     # 允许手动触发
  release:
    types: [published]   # 当发布时触发

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: immortalwrt  # ✅ 所有命令在该目录执行

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare environment
        run: |
          # 修正换行符并确保脚本可执行
          sed -i 's/\r$//' docker-build.sh build.sh || true
          chmod +x docker-build.sh build.sh
          # 创建输出目录
          mkdir -p bin files
          sudo chmod -R 777 bin files

      - name: Pull ImmortalWRT ImageBuilder
        run: docker pull immortalwrt/imagebuilder:armsr-armv8-openwrt-24.10  # ✅ 正确镜像

      - name: Build firmware
        run: ./docker-build.sh  # ✅ 执行构建脚本

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-images
          path: immortalwrt/bin/

      # 🕒 获取北京时间（UTC+8）
      - name: Get Beijing Time
        id: time
        run: |
          export TZ=Asia/Shanghai
          echo "datetime=$(date '+%Y-%m-%d %H:%M:%S %Z')" >> $GITHUB_OUTPUT

      - name: List built files
        id: list_files
        run: |
          echo "files=$(ls -1 bin/ | sed 's/^/  - /')" >> $GITHUB_OUTPUT

      # 🚀 发布 Release
      - name: Publish to GitHub Release
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/main'
        with:
          tag_name: build-${{ github.run_number }}  # ✅ 保留独立编号
          name: "N1 ImmortalWrt"  # ✅ 固定标题
          body: |
            ✅ N1 ImmortalWrt 固件自动构建完成！

            - 分支：${{ github.ref_name }}
            - 构建编号：${{ github.run_number }}
            - 发布时间（北京时间）：${{ steps.time.outputs.datetime }}

            以下为本次构建生成的固件文件：
            ```bash
            ${{ steps.list_files.outputs.files }}
            ```
          files: immortalwrt/bin/**
          overwrite_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 🧹 清理旧版本，仅保留最近 1 个
      - name: Clean up old releases
        if: github.ref == 'refs/heads/main'
        uses: dev-drprasad/delete-older-releases@v0.3.4
        with:
          keep_latest: 1
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
